# .env.example - Environment Variable Template
# Copy this file to .env and update with your actual values
# NEVER commit .env to git (it's in .gitignore)

# Flask API Configuration
PORT=5000
FLASK_ENV=production
FLASK_DEBUG=0

# Logging Configuration
LOG_DIR=/app/logs
LOG_LEVEL=INFO

# MLflow Configuration
MLFLOW_TRACKING_URI=http://mlflow:5001
MLFLOW_BACKEND_STORE_URI=file:///mlflow/mlruns
MLFLOW_DEFAULT_ARTIFACT_ROOT=file:///mlflow/artifacts

# DVC Configuration (if pulling data in container)
# DVC_ACCESS_KEY_ID=your_access_key_here
# DVC_SECRET_ACCESS_KEY=your_secret_key_here

# Docker Hub Configuration (for publishing)
# DOCKER_USERNAME=your_dockerhub_username
# DOCKER_REGISTRY=docker.io
